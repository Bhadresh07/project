<% include ./partials/header %>
<div class="container">
	<% for(let i = rev.length-1; i >=0 ; i--) { %>
  <div class="re">
		<article>
			<h2><strong>
					<%=rev[i].cname%></strong></h2>
			<h3><strong>
					<%=rev[i].title%></strong></h3>
					<%=rev[i].createdAt%>
			<p class="para">
				<%= rev[i].review %>
			</p>
		</article>
	</div>
	<% } %>
</div>
</center>
<% include ./partials/footer %>
